using Database.Entity;
using ModuleUI.Managers;

namespace UnitTests
{
    [TestClass]
    public class DialogManagerCallerTest
    {/*
        EmployeeDialogManager employeeDialogManagerForZero = new EmployeeDialogManager();
        DepartmentDialogManager departmentDialogManagerForOne = new DepartmentDialogManager();
        PositionDialogManager positionDialogManager = new PositionDialogManager();

        Employee empl = new Employee();
        Department dep = new Department();
        Position pos = new Position();


        [TestMethod]
        public void RegisterForTabNumberTest()
        {
            DialogManagerCaller.RegisterDialogManagerForTabNumber(employeeDialogManagerForZero, 0);
            DialogManagerCaller.RegisterDialogManagerForTabNumber(departmentDialogManagerForOne, 1);
            DialogManagerCaller.RegisterDialogManagerForTabNumber(positionDialogManager, 3);

            int tabNumber = 0;
            Assert.AreEqual(employeeDialogManagerForZero, tabNumber.GetDialogManager());

            tabNumber = 1;
            Assert.AreEqual(departmentDialogManagerForOne, tabNumber.GetDialogManager());

            tabNumber = 3;
            Assert.AreEqual(positionDialogManager, tabNumber.GetDialogManager());
        }

        [TestMethod]
        public void RegisterForTabEntityTest()
        {
            DialogManagerCaller.RegisterDialogManagerForEntity(employeeDialogManagerForZero, empl.GetType());
            DialogManagerCaller.RegisterDialogManagerForEntity(departmentDialogManagerForOne, dep.GetType());
            DialogManagerCaller.RegisterDialogManagerForEntity(positionDialogManager, pos.GetType());

            Assert.AreEqual(employeeDialogManagerForZero, empl.GetDialogManager());
            Assert.AreEqual(departmentDialogManagerForOne, dep.GetDialogManager());
            Assert.AreEqual(positionDialogManager, pos.GetDialogManager());
        }*/
    }
}
